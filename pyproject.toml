[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eitherutil"
version = "0.1.0"
description = "Implementation of a Either type with Failure (left) / Success (right) value and support implementation for shutil, pathlib, subprocess and tkinter functions."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []